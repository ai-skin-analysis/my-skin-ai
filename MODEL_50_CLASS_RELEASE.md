# การเปิดใช้โมเดลคัดกรอง 50 โรค

สถานะ `active` ของระบบหมายถึงโรคที่เป็น output จริงของไฟล์โมเดล และผ่าน release gate แล้วเท่านั้น ไม่ได้หมายถึงโรคที่มีชื่ออยู่ใน `disease_catalog.json`.

## สถานะปัจจุบัน

ไฟล์ `models/skin_disease_model.h5` ที่มากับโครงการมี output 6 คลาส จึงไม่สามารถเปิดใช้เป็นโมเดล 50 โรคได้ และ production จะไม่รับภาพใด ๆ จากไฟล์นี้

รายการ 6 output เดิมยังไม่ตรงกับ catalog 50 โรคฉบับปัจจุบัน (เช่น `Benign keratosis` และ `Vascular lesions` เป็นกลุ่มกว้าง ไม่ใช่ label เป้าหมายใหม่) จึงห้ามแมปหรือเปลี่ยนชื่อ output เดิมเพื่อให้ดูเหมือนรองรับรายการใหม่.

## เงื่อนไขก่อนเปิดใช้ครบ 50 โรค

1. สร้างข้อมูลฝึกที่ได้รับอนุญาตครบทั้ง 50 `id` ใน `disease_catalog.json` พร้อม `dataset_manifest.json` ที่ระบุแหล่งที่มา ใบอนุญาต และบันทึกอนุมัติการใช้ข้อมูล
2. ฝึกไฟล์โมเดลใหม่ให้มี output 50 ค่า โดย `class_ids` และ `class_names` ใน metadata ต้องตรงกับ catalog ทุกตัวและตรงตามลำดับ output
3. สำหรับชุดข้อมูลที่รวมภาพถ่ายทั่วไปกับ dermoscopy ให้รัน `train_multiclass.py --group all --allow-mixed-domains` เพื่อให้ metadata ระบุ routing ของชนิดภาพ ระบบจะบังคับให้ผู้ใช้เลือกชนิดภาพเองและจะไม่เดาจากรูป
4. ทำ independent evaluation ที่แยกผู้ป่วย/รอยโรคออกจากชุดฝึก พร้อมผลรายโรคและผลตามชนิดภาพ
5. ปรับเทียบคะแนน, ทดสอบภาพนอกขอบเขต, บันทึก intended use และให้ผู้รับผิดชอบทางคลินิกอนุมัติ release
6. เก็บ artifact SHA-256 และ metadata ที่ผ่านการตรวจไว้ด้วยกัน แล้วตั้งค่าระบบ production ตาม `PRODUCTION_DEPLOYMENT.md`

ระบบตรวจทุกข้อข้างต้นเมื่อเริ่มต้น production รวมถึงตรวจว่า output จริงของ Keras มี 50 ค่า หากขาดข้อใด ระบบจะปิดการอัปโหลดภาพแทนที่จะให้ผลจากโมเดลที่ไม่ครบขอบเขต

เกณฑ์ 5 ภาพต่อคลาสที่แสดงในหน้า Admin เป็นเพียงเกณฑ์เริ่มงานฝึก candidate เพื่อทดสอบ pipeline เท่านั้น ไม่ใช่เกณฑ์เปิดใช้โมเดล และไม่ลดเงื่อนไขการประเมินอิสระหรือการอนุมัติทางคลินิกข้างต้น

## สิ่งที่ไม่ควรทำ

- ห้ามคัดลอกหรือทำซ้ำ output 6 คลาสเพื่อให้ครบ 50 ชื่อ
- ห้ามแก้ metadata หรือ catalog เพื่อบอกว่าโรค active ทั้งที่ไฟล์โมเดลไม่มี output ของโรคนั้น
- ห้ามนำ `archive.zip` มาใช้ฝึกจนกว่าจะยืนยันแหล่งที่มา สิทธิการใช้ภาพ ฉลากโรค และบันทึกอนุมัติ
